apiVersion: apps/v1
kind: Deployment
metadata:
  name: n8n
  namespace: preme-n8n
spec:
  template:
    spec:
      containers:
      - name: n8n
        env:
        - name: NODE_FUNCTION_ALLOW_EXTERNAL
          value: "puppeteer-extra,puppeteer-extra-plugin-stealth,puppeteer-core,crawlee"
        envFrom:
        - secretRef:
            name: scraper-creds
